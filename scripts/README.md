`minify.js` rewrites `.html`, `.css`, `.js`, and `.json` files in place.

Usage:

```bash
node scripts/minify.js public
node scripts/minify.js src public
```

If no path is passed, it defaults to `public`.
